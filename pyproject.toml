[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "insurance-gam"
version = "0.1.3"
description = "Interpretable GAM toolkit for insurance pricing — EBM, Neural Additive Models, and Pairwise Interaction Networks"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Burning Cost", email = "pricing.frontier@gmail.com" }]
keywords = [
    "insurance",
    "pricing",
    "actuarial",
    "GAM",
    "EBM",
    "neural-additive-model",
    "interpretable-ml",
    "pairwise-interactions",
    "ga2m",
    "poisson",
    "tweedie",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

# Core dependencies shared across all three subpackages
dependencies = [
    "numpy>=1.24.0",
    "polars>=1.0",
    "matplotlib>=3.7.0",
    "scikit-learn>=1.3.0",
    "pandas>=2.0.0",
    "pyarrow>=10.0.0",
]

[project.optional-dependencies]
# Neural subpackages (anam, pin)
neural = ["torch>=2.0.0"]
# EBM subpackage
ebm = ["interpret>=0.7.0"]
# Optional EBM extras
excel = ["openpyxl>=3.0"]
glm = ["statsmodels>=0.14.5"]
# Everything
all = [
    "torch>=2.0.0",
    "interpret>=0.7.0",
    "openpyxl>=3.0",
    "statsmodels>=0.14.5",
]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.0.0",
    "databricks-sdk>=0.97.0",
]

[project.urls]
Homepage = "https://github.com/burning-cost/insurance-gam"
Repository = "https://github.com/burning-cost/insurance-gam"

[tool.hatch.build.targets.wheel]
packages = ["src/insurance_gam"]

[tool.pytest.ini_options]
testpaths = ["tests/ebm", "tests/anam", "tests/pin"]
addopts = "-v --tb=short"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "torch>=2.10.0",
]
